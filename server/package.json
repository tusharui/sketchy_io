{
	"name": "server",
	"scripts": {
		"dev": "bun run --hot src/index.ts",
		"format": "biome format",
		"lint": "biome lint",
		"check": "biome check --write"
	},
	"dependencies": {
		"@socket.io/bun-engine": "^0.1.0",
		"socket.io": "^4.8.1"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.8",
		"@types/bun": "latest"
	}
}
